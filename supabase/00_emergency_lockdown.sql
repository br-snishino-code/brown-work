-- ============================================================
-- 緊急対応：今すぐ実行してください（Supabase Auth移行前の暫定処置）
-- ============================================================
-- 現状、public.app_state テーブルは anon キーに対して
-- 「誰でも全件読み書き可能」なポリシーになっています。
-- anonキーはフロントエンドのJSに埋め込まれる前提のキーなので、
-- 現在のURLとキーを知っていれば誰でも全社員のパスワード（平文）を
-- 含む勤怠データを読み書きできる状態です。
--
-- このSQLは、Supabase Auth移行が完了するまでの「一時的な鍵閉め」です。
-- 実行すると、anonキーからの app_state 読み書きが停止します。
-- → アプリはクラウド同期に失敗しますが、端末のlocalStorageには
--    引き続き保存されるため、アプリ自体は動き続けます（同期が止まるだけ）。
--
-- Supabaseダッシュボード → SQL Editor に貼り付けて実行してください。
-- ============================================================

drop policy if exists "brown work test read" on public.app_state;
drop policy if exists "brown work test write" on public.app_state;

-- 新しいポリシーは意図的に作成しません（＝anon/authenticatedからの
-- 直接アクセスを全面禁止）。service_role キー（サーバー側のみ）だけが
-- このテーブルにアクセスできる状態になります。
